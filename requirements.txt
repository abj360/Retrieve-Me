# retrieval-core direct dependencies, with version floors.
# Install with:  pip install -r requirements.txt
# For a fully pinned, reproducible install use requirements.lock instead.

# web framework
fastapi>=0.115.4
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2

# storage
qdrant-client>=1.12.0
redis>=5.2.0

# ml / retrieval
sentence-transformers>=3.1.1
numpy>=2.1.3
rank-bm25>=0.2.2

# config
pyyaml>=6.0.2

# test + dev tooling (also installable via: pip install -e ".[dev]")
httpx>=0.27.2
pytest>=9.0.3
ruff>=0.7.2
mypy>=1.13.0
types-PyYAML>=6.0.12
pip-audit>=2.7.3
